# pytest.ini
[pytest]
# Inserta la carpeta del proyecto (punto .) en sys.path
pythonpath = .
addopts = -q --cov=devops_testing --cov-report=term-missing --cov-fail-under=85
markers =
    benchmark: benchmarks de rendimiento
    property:  tests property-based
